#include <sys/socket.h>
#include <linux/if_packet.h>
#include <net/ethernet.h>

#include "util.h"

int32_t
main(int32_t argc, char *argv[])
{
    int32_t sockfd;

    INFO("base frequency = %u [kHz]", BASE_FREQ);

    return 0;
}

